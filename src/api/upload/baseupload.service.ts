import { UniversalsService } from '../../@core/common/universals.service';

export class BaseUploadService extends UniversalsService {
  protected getUploadError(multi: boolean, req: any, err: any): any {

  }
}
